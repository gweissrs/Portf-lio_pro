import styles from './Skills.module.css';

// Skills: grid de ícones + nomes por categoria (Front-end, Back-end, Ferramentas),
// sem barras de progresso, animação de entrada em stagger com Framer Motion
export function Skills() {
  return (
    <section id="skills" className={styles.skills}>
      <p className={styles.placeholder}>Seção Skills — será implementada no próximo prompt</p>
    </section>
  );
}
