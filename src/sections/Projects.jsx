import styles from './Projects.module.css';

// Projetos: grid de cards com imagem, nome, descrição, stack tags,
// links para GitHub e ao vivo, hover com elevação e glow ciano
export function Projects() {
  return (
    <section id="projetos" className={styles.projects}>
      <p className={styles.placeholder}>Seção Projetos — será implementada no próximo prompt</p>
    </section>
  );
}
