import styles from './Hero.module.css';

// Hero: nome animado, título, CTA para projetos e contato,
// scroll indicator animado, background com gradiente radial ciano sutil
export function Hero() {
  return (
    <section id="hero" className={styles.hero}>
      <p className={styles.placeholder}>Seção Hero — será implementada no próximo prompt</p>
    </section>
  );
}
